using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This is a configuration file where you can set the input parameters.
/// </summary>
public class main {

    static public float a1 = 1; // arm 1 length (closest to base)
    static public float a2 = 3; // arm 2 length (furthest from base)
    static public float a3 = 1; // arm 3 length (the vertical z-axis end)

    static public float x0 = 0; // Base coordinates (x)
    static public float y0 = 0; // Base coordinates (y)
    static public float z0 = 2.5f; // Base Fixed Height (z)

}
